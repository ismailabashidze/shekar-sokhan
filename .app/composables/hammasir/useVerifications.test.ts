// Test file to verify useVerifications composable
import { useVerifications } from './useVerifications'

// This is just to verify the import works correctly
console.log('useVerifications imported successfully')

// Export for testing
export { useVerifications }